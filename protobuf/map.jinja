{% import_yaml "protobuf/defaults.yaml" as defaults %}
{% import_yaml 'protobuf/osfamilymap.yaml' as osfamilymap %}

{% set protobuf = salt['grains.filter_by'](
    defaults,
    merge = salt['grains.filter_by'](
        osfamilymap,
        grain='os_family',
        merge = salt['pillar.get']('protobuf', {}),
    ),
    base='protobuf')
%}
